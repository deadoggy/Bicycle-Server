{
  "tasks": [
    {
      "type": "shell",
      "label": "gcc build active file",
      "command": "/usr/bin/gcc",
      "args": [
        "-g",
        "${fileDirname}/rio.c",
        "${fileDirname}/bicyclewebserv.c",
        "${file}",
        "-o",
        "${fileDirname}/../target/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}